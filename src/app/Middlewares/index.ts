/**
 * Middleware
 * -------------------------------------------
 * Gather all middleware from all aspects
 * -------------------------------------------
 */
export { default as isAuthenticated } from './is-authenticated';
export { default as redirectLoggedUser } from './redirect-when-logged-in';